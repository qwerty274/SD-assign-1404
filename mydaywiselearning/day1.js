const button=document.querySelector('button'); 
button.addEventListener('click', () => {
    alert('Button clicked!');
});
//querySelector helps us search the query in the whole document
//document is said to be as the current webpage